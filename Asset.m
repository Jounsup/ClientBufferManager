classdef Asset
    properties (SetAccess = public)
        manifest = manifest;
        submanifest = submanifest;
    end
    methods 
%         function A = Asset(manifest, submanifest)
%             A.manifest = manifest;
%             A.submanifest = submanifest;
%         end
    end
end